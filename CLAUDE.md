# tjg-claude-marketplace

When I ask you to merge PR execute `gh pr merge --squash --delete-branch`.

## Agent skills

### Issue tracker

Issues tracked in GitHub Issues (TheJavaGuy/tjg-claude-marketplace) via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
