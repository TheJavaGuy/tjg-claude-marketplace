# Enforces markdown formatting at push time. Check-only: the hook never
# rewrites files — run `npm run format` to fix what it reports.
pre-push:
  jobs:
    - name: markdown format check
      run: git ls-files -z '*.md' | xargs -0 -r npx prettier --check
